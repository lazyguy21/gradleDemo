allprojects {
    group = 'org.yyf'
    version = '0.1'

    repositories {
//        mavenLocal()
        mavenCentral()
    }

}
subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'//发布到maven仓库用的plugin
}

