allprojects {
    group = 'org.yyf'
    version = '0.1'

    repositories {
        mavenCentral()
    }

}
subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'//发布到maven仓库用的plugin
}

